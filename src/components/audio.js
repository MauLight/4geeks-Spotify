import React from 'react';
import AudioPlayer from './audioplayer';

const Audio = () => {

    const audioElement = new Audio("../mp3/01.mp3");

/*
    audioElement.play();
    audioElement.pause();

    audioElement.currentTime;
    audioElement.ended;
    audioElement.duration;
*/

}

export default Audio;